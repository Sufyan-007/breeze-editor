- change APP_CONFIG_PATH variable value in app_consts.py to the directory where all the configuration are saved for the project

- change path of app_basic_config.json file to where you want to store the generated project

- run python app_generator.py to generate project
